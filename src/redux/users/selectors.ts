import { createSelector } from "@reduxjs/toolkit";
import { RootState } from "redux/rootReducer";
import { UsersState } from "./types";



// export const selectUsersState = (state: RootState): UsersState => state.users;

// export const selectUserData = createSelector(selectUsersState, (users) => users.user);

// export const selectUsersError = createSelector(selectUsersState, (users) => users.error);
